version: "3"
services:
  backend:
    build:
      context: ./backend
    ports:
      - 80:5000
    volumes:
      - ./backend:/backend
    networks:
      - slproposals_docker_network
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: casper
      MONGO_INITDB_ROOT_PASSWORD: meow
      MONGO_INITDB_DATABASE: slproposals
    ports:
      - 27017:27017
    networks:
      - slproposals_docker_network
    volumes:
      - './db_data:/data/db'
      - './db_config:/data/configdb'
      - './docker-entrypoint-initdb.d/mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro'

volumes:
  backend:
    driver: local
  db_data:
    driver: local
  db_config:
    driver: local
  docker-entrypoint-initdb.d:
    driver: local

networks:
  slproposals_docker_network:
    driver: bridge